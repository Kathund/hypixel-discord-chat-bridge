allow management commands like /kick to return the message that gets added to log
see bad impl - https://github.com/Kathund/Hypixel-Discord-Guild-Chat-Bridge/blob/main/src/Minecraft/Handlers/MessageHandler.ts#L131-L135

allow modes to be inputed in duels command

debug doesn't work

port web
port tests
port inactivity
port hypixel events
port skyblock events Notifications
port help discord command
port info discord command

discord command alias
Make buttons on linked command timeout after 2 minutes since last use

Config
Config Schema
Validation from that schema with full type checking
Update github pages with all the moved/new config shit

Updater
Has modes
  rolling - latest commit
  tag/version - uses git tags and effectivly is a release system
alerts via discord that there is a new update with one click pull and restart

i18n? Maybe? Probs out of scope of typescript but something I want to look into
